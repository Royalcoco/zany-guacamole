const express = require('express')
const app = express()
const port = 3000

app.get('/', (req, res) => {
  res.send('Hello World!')
})

app.listen(port, () => {
  console.log(`Example app listening on port ${port}`)
})
if (condition) {
  @Template juxon-junior_files.for reason unknown <<comments">>
  junior_files.for reason unknown pursuit <<comments">> Error: " <<
  find duplicate <<comments">> random choice <<comments">> " << find application // find src <<comments applicated on resssource #TRASH ;>> --------------------------------
  random somme <<comments">> equal's <<comments">> multiplie by ²1&&1-(| -".join  } {]).x}
  
}
reason = ^/ *|musltiplie ('>"]